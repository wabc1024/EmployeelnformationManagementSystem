<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wabc.mapper.EmployeeMapper">


    <select id="queryAllEmpInfo" resultType="employeeInfo">
        SELECT e.emp_id,e.name,e.salary,d.department_name,mgr.name as 'managerName'
        FROM employees e LEFT JOIN departments d
        ON e.department_id = d.department_id
        LEFT JOIN employees mgr
        ON e.manager_id = mgr.emp_id
    </select>

    <select id="queryIdByName" resultType="int">
        select emp_id from employees
        where name = #{empName}
    </select>
</mapper>